#include <bits/stdc++.h>
using namespace std;
int main(){
    int tc;
    cin>>tc;
    while(tc--){
        int n;
        cin>>n;
        string s=to_string(n);
        int x=s.length();
        for(int i=0;i<x;i++){
            
        }
        
    }
    return 0;
}